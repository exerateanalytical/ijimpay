- [MUST] docs/16-flows-catalog.md :: F-040/F-041 (resend request, change number mid-flow) require cancelling a pending charge and creating a new one, but no charge-cancel/resend endpoint exists in docs/02 and none is declared in the "API additions needed" table (API-ADD-1..16) :: add an API-ADD row (e.g. `POST /charges/{id}/cancel` per docs/15, or `POST /v1/public/charges/{id}/resend` per docs/14) and align the three docs on one path
- [MUST] docs/16-flows-catalog.md :: Conventions line 4 declares `draft` as one of "the 8 canonical" StatusBadge statuses, contradicting docs/07 §5 (which lists succeeded/pending/failed/expired/refunded/processing/pending_approval and no draft) and docs/12 line 9 / docs/13 line 8 which both call draft a non-StatusBadge ink-500 pill :: reword to 7-status-plus-draft-pill exactly as docs/12/13 do, or fix docs/07 §5 to enumerate the 8th
- [MUST] docs/16-flows-catalog.md :: F-015 bulk CSV payout caps the file at "≤ 1 000 lignes v1" but docs/12 D-20 (En masse) validates "≤ 5 000 lignes" :: pick one limit and align both docs
- [MUST] docs/16-flows-catalog.md :: F-047 Tier-2 upgrade requires ops maker + ops-admin approver (dual-control, citing docs/08 §B), but docs/13 OM-02 lets a single compliance user approve any tier (DC only for custom limits) :: make docs/13 OM-02 tier-2 approval a DC request, or correct F-047's actor rules
- [MUST] docs/16-flows-catalog.md :: F-045 says "ops" approves KYB, but docs/13 O-03/O-04 make ops read-only and reserve approve/reject for compliance (and ops-admin) :: align actor roles between F-045/F-046 and O-04
- [MUST] docs/16-flows-catalog.md :: F-035 step 3 calls docs/02 `POST /charges` "publishable-key context", but docs/14 declares a distinct `POST /v1/public/charges` (server-brokered) in its API additions — the payer flow references an endpoint shape that docs/14 says does not exist for this surface :: reference docs/14's public endpoints (or add them to F-016's API-ADD table)
- [MUST] docs/16-flows-catalog.md :: F-019 alternate path "internal transfer from merchant_available to wallet" (`[API-ADD-7] POST /balance_transfers`) has no corresponding UI anywhere in docs/12 (D-29/DM-16 only cover the external-deposit path) :: add the internal-transfer option to DM-16 or D-29, or drop the alt path
- [MUST] docs/16-flows-catalog.md :: F-016 scheduled payroll (`[API-ADD-5]` schedule endpoints, "sets schedule day of month/time") has no schedule UI in docs/12 — DM-30 payroll-list drawer has no schedule field and D-25 has no scheduling action :: add schedule inputs to DM-30 or mark F-016 as post-v1
- [MUST] docs/16-flows-catalog.md :: F-024 key rotation with merchant-chosen grace window (0/24 h/72 h, "Ancienne clé expire le {date}" banner) is absent from docs/12 D-32/DM-18, which only spec create + revoke :: add a rotation flow to D-32 or remove the grace-window promise from F-024
- [MUST] docs/16-flows-catalog.md :: F-033 account recovery ("Je n'ai plus accès à mon numéro" entry on /forgot-password, `[API-ADD-13]`) has no entry point in docs/12 D-04 and no recovery screen exists in D-01..D-48 :: add the recovery entry/flow to docs/12 or re-scope F-033
- [MUST] docs/16-flows-catalog.md :: F-007/F-068 go-live checklist modal shown before flipping Test/Live (checklist stored via `[API-ADD-16]`) is missing from docs/12 — the §0.2 toggle only has a tier-0 disabled tooltip, no checklist modal :: spec the go-live modal in docs/12 (new DM) or drop it from F-007/F-068
- [MUST] docs/16-flows-catalog.md :: F-003 forgot-password is OTP-based (enter OTP then set password, TOTP also required for enrolled users) while docs/12 D-04/D-05 spec a token-link flow (`/reset-password?token=`) with no OTP step and no TOTP requirement :: reconcile the reset mechanism and add the TOTP-before-reset rule to D-05
- [MUST] docs/16-flows-catalog.md :: Auth API additions diverge three ways — 16 API-ADD-1 (`POST /auth/otp`, `/auth/login`, `/auth/password_reset`), docs/12 §3 (`/auth/signup/otp`, `/auth/login/totp`, `/auth/password/forgot|/reset`, `/auth/step_up`), docs/15 (`/auth/otp/request`, `/auth/pin/set`, `/auth/token/refresh`, `/auth/logout`) :: converge on one auth path set across the three "API additions" lists
- [MUST] docs/15-mobile-app-screens-spec.md :: Push routing table uses event types `payout_batch.pending_approval` and `payout_batch.partially_failed` which exist neither in docs/02 §2.7 nor in the doc's own "Push event types" additions (only kyb.decision, security.new_device, team.member_changed are declared) :: declare both push types under API additions or route from N-12/N-13 equivalents
- [MUST] docs/15-mobile-app-screens-spec.md :: Team API paths (`POST /invitations`, `POST /invitations/{id}/resend`) contradict docs/12 (`POST /invites`, `/invites/{id}/resend`) and docs/16 API-ADD-8 (`POST /invites`) :: rename to /invites to match the other two docs
- [MUST] docs/14-checkout-pages-spec.md :: `repeat` icon is used for retry/resend on C-09, C-11, C-12, C-13, C-22 and CM-03, but docs/07 §4 maps `repeat` to Subscription and its rules forbid two meanings per icon; docs/12/13/15/16 all use `rotate-cw` for retry; `repeat` is not in docs/14's icon-additions list either :: switch all retry/resend actions to `rotate-cw`
- [MUST] docs/12-dashboard-pages-spec.md :: D-28/DM-15 use the `clock` icon for the pause/resume subscription action — `clock` is the canonical Pending status icon and docs/07 §4 forbids repurposing status icons; docs/16 icon additions reserve `circle-pause`/`circle-play` for exactly this :: use circle-pause/circle-play and add them to docs/12's icon list
- [MUST] docs/12-dashboard-pages-spec.md :: DM-16 hardcodes top-up USSD steps "1. Composez #150# (MTN) ou #144# (Orange)" which contradict docs/16 F-019 ("*126#" for the same flow) and violate the config-driven/never-hardcoded rule stated in docs/09 and docs/14 (MTN's verified code family is *126…) :: replace with config-served instructions and align codes with F-019/docs/14
- [MUST] docs/12-dashboard-pages-spec.md :: DM-09 batch-reject reason validates 5–300 chars and docs/15 MS-09 validates 5–200, while docs/16 F-018 requires ≥ 10 chars (and docs/13's DC rule is ≥ 10) :: align the minimum (10) and max across 12/15/16
- [MUST] docs/12-dashboard-pages-spec.md :: export download link validity is 48 h (D-12 "expire 48 h", DM-35 "lien 48 h") but docs/16 F-013/N-09 promise a 72 h signed URL :: pick one TTL for both docs
- [MUST] docs/16-flows-catalog.md :: API-ADD paths conflict with docs/12's additions for the same actions — API-ADD-9 `POST /api_keys/{id}/revoke` vs docs/12 `DELETE /api_keys/{id}`; API-ADD-10 `POST /events/{id}/redeliver` vs docs/12 `POST /webhook_deliveries/{id}/redeliver`; API-ADD-11 `POST /invoices/{id}/dunning_link` vs docs/12 `POST /invoices/{id}/send_link`; API-ADD-12 `POST /devices/{id}/revoke` vs docs/12 `DELETE /me/devices/{id}` vs docs/15 `DELETE /devices/{id}` :: converge each pair on one method+path
- [MUST] docs/16-flows-catalog.md :: F-052 says the analyst "can apply holds (payouts locked)" during a risk case, but docs/13 O-13 has no hold/lock action anywhere (actions are note, attach, close, STR pack) :: add a payout-hold action to O-13 (with DC if needed) or remove holds from F-052
- [MUST] docs/16-flows-catalog.md :: Icon additions list `flag` for risk flag/case, but docs/13 (the ops surface where risk lives) adds `siren` for the same concept — two icons for one meaning across the set :: standardize on `siren` (already used by O-12/O-13) and drop `flag`
- [MUST] docs/16-flows-catalog.md :: F-015 API-ADD gap — step 5 shows fees on the review screen and step 3 runs server-side row validation, but only docs/12 declares `GET /fees` and `POST /payout_batches/validate`; docs/16's own API-ADD table omits both while claiming "none invented inline" :: add the two endpoints to the API-ADD table (referencing docs/12's names)
- [MINOR] docs/16-flows-catalog.md :: F-021 screen reference "app `team-invite`" — no such screen id exists in docs/15 (invite is sheet MS-10 from M-40 `team-list`); F-020 references "app `settlements-detail`" while docs/15's route is `settlement-detail` (M-39) :: fix both slugs to MS-10/M-40 and `settlement-detail`
- [MINOR] docs/12-dashboard-pages-spec.md :: DM-02 refund reason list (Erreur de montant / Produit indisponible / Demande du client / Autre) differs from docs/16 F-012 (Client insatisfait / Erreur de montant / Doublon / Autre) and docs/15 MS-07 (Erreur de montant / Client insatisfait / Doublon / Autre) :: use one canonical reason list on all three surfaces
- [MINOR] docs/12-dashboard-pages-spec.md :: D-13/DM-07 restrict link deactivation to Owner/Admin, but docs/16 F-011 allows "creators may deactivate their own" and docs/15 M-21/M-24 allow Owner/Admin/Finance :: align deactivation roles across 12/15/16
- [MINOR] docs/12-dashboard-pages-spec.md :: D-28 claims pause/cancel emit "événements `subscription.*` webhooks", but docs/02 §2.7 only defines subscription.past_due and subscription.canceled — no subscription.paused/resumed event exists and none is declared in the additions :: limit the claim to subscription.canceled or declare the new event types
- [MINOR] docs/14-checkout-pages-spec.md :: C-02 open-amount max is 5 000 000 FCFA while docs/12 D-14/DM-04 allow amounts up to 10 000 000 FCFA for links/charges — an open link min/max policy inconsistent with the dashboard's creation limits :: align payer-side max with the dashboard cap or state the payer-side cap in docs/12
- [MINOR] docs/14-checkout-pages-spec.md :: icon additions declare `phone` for calling the merchant (CM-05) while docs/16's icon additions declare `phone-call` for voice-call fallback — two icons for the call concept :: standardize on one Lucide name in both lists
- [MINOR] docs/14-checkout-pages-spec.md :: C-11 offers plain "Réessayer" for every failure_code, but docs/16 F-039 requires retry to be disabled for `payer_not_found` on the same number+channel pair (forcing a change of number) :: add the payer_not_found retry-disable rule to C-11
- [MINOR] docs/13-ops-console-spec.md :: O-01 uses `log-out` ("miroir") as the SSO sign-in icon, repurposing the docs/07 sign-out icon; docs/12 already adds `log-in` for exactly this :: use `log-in` and add it to docs/13's icon-additions list
- [MINOR] docs/13-ops-console-spec.md :: O-09/OM-13 use `download` ("sens import") for report import while docs/12 and docs/16 introduce `upload`/`file-up` for imports — one icon, two directions :: use `upload` for import actions and add it to the icon-additions list
- [MINOR] docs/13-ops-console-spec.md :: O-16/OM-10 account picker lists only the 7 docs/03 chart-of-accounts entries, but docs/16 F-014 posts through a `payout_inflight` clearing account (docs/03 mentions it only in prose, not the chart) :: add payout_inflight to the picker/chart or note the omission in data-model additions
- [MINOR] docs/13-ops-console-spec.md :: docs/16 F-054 promises "no hardware key within 7 days → account auto-disabled", but O-19 has no such auto-disable rule (only "doit enrôler une clé à la première connexion") :: add the 7-day auto-disable to O-19/O-20 or remove it from F-054
- [MINOR] docs/15-mobile-app-screens-spec.md :: M-43 device enrollment ("Autoriser cet appareil pour les validations" with OTP) omits the 24 h seasoning delay required by docs/16 F-031 before approvals are allowed on a new device :: state the 24 h seasoning in M-43/M-31
- [MINOR] docs/15-mobile-app-screens-spec.md :: INVENTORY `tables=69` counts every markdown table (69 `|---` blocks) whereas docs/12 (tables=32 = UI **Tableau** count) and docs/13 (explicit UI-table rule) count rendered data tables — the mobile app renders essentially zero data tables, so 69 is not comparable and no counting rule is stated :: state the counting rule or recount as UI tables
- [MINOR] docs/15-mobile-app-screens-spec.md :: monthly export uses `GET /exports/charges?month=` while docs/12 and docs/16 API-ADD-4 declare `POST /exports` + `GET /exports/{id}` for the same job :: reuse the /exports job endpoints
- [MINOR] docs/16-flows-catalog.md :: F-012 refund modal reason list and copy state "fee not returned v1", and DM-02 (docs/12) enforces refund ≤ net of the charge but not "net minus prior refunds" as F-012 requires for partial-refund sequences :: make DM-02's cap the un-refunded remainder
- [MINOR] docs/16-flows-catalog.md :: F-034 "Créer une nouvelle entreprise" maps to F-001 steps 4–5, but docs/12 DM-29 sends it through "flux type D-02 étape entreprise → onboarding" and docs/16 icon additions give the switcher `building-2` while neither docs/12 DM-29 (`plus-circle`) nor docs/15 M-12 (`chevron-right`) uses it :: align switcher icon and the new-business entry flow
