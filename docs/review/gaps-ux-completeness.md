# UX-State Completeness Review — docs 12–16

- [MUST] docs/12-dashboard-pages-spec.md :: D-23 Payout review (Individuel path) — "Confirmer le paiement" for a single payout has no 2FA/step-up and the button does not restate amount + beneficiary, while docs/15 M-34 requires MS-08 biometric/PIN for the same action and docs/07 §5 requires restating amount + beneficiary for money-moving confirms :: add a confirm step to D-23 (single path) restating "Envoyer {montant} FCFA à {nom} ({numéro}) ?" + DM-28 2FA re-prompt, matching the mobile bar
- [MUST] docs/12-dashboard-pages-spec.md :: D-23 single-payout path — no post-submit destination or success/failure screen is specified (only the batch path states "→ D-24"); provider-failure of `POST /payouts` at submit has no UI state :: specify where the user lands after single `POST /payouts` (payout detail or D-19 with toast) and the failure/`provider_error` state
- [MUST] docs/12-dashboard-pages-spec.md :: No suspended-merchant state anywhere in the authenticated dashboard, yet docs/13 OM-05 states "le dashboard passe en lecture seule" and F-048 shows a suspension banner "Compte suspendu — contactez le support" :: add a global suspended state (§0.3 banner + read-only enforcement, all money actions hidden) to the global chrome section
- [MUST] docs/12-dashboard-pages-spec.md :: No spec for the ops read-only impersonation session, yet docs/13 OM-06 opens app.ijimpay.com with a "bandeau danger-600 « Session ops — lecture seule »" that doc 12 (which owns app.ijimpay.com) never defines :: add an impersonation-session global state (danger banner, all mutations disabled, 30-min expiry behavior)
- [MUST] docs/12-dashboard-pages-spec.md :: No page or setting exists for configuring the maker–checker approval threshold, yet PRD docs/01 §4 requires "configurable thresholds" and D-23/F-014 reference "si seuil dépassé" :: add threshold configuration (e.g. in /settings/business or a payouts settings modal, Owner/Admin, 2FA)
- [MUST] docs/12-dashboard-pages-spec.md :: D-34 Webhook endpoint detail lacks the "Réactiver" action for auto-disabled endpoints required by F-027/F-058 (docs/16) and PRD 08 §A9 (7-day auto-disable → re-enable + redeliver) :: add a disabled-state banner with [Réactiver] action (`POST /webhook_endpoints/{id}/enable`) to D-34
- [MUST] docs/12-dashboard-pages-spec.md :: DM-30 Payroll list drawer has no schedule fields, yet F-016 (docs/16) specifies a scheduled payroll run set up by Finance ("le 28 du mois", API-ADD-5 `POST /payroll_lists/{id}/schedule`) :: add schedule inputs (day of month, time, enable toggle) + validation + FR errors to DM-30, or declare scheduling out of v1 in both docs
- [MUST] docs/12-dashboard-pages-spec.md :: DM-16 Top-up modal omits the internal-transfer path (available → payout wallet) specified as F-019 alt path (`POST /balance_transfers`, instant, confirm restating amount) :: add the internal-transfer option to DM-16 with a confirm restating amount and a failure state, or remove the alt path from F-016/F-019
- [MUST] docs/12-dashboard-pages-spec.md :: D-04 Forgot password has no "Je n'ai plus accès à mon numéro" account-recovery entry, yet F-033 (docs/16) hangs the entire recovery flow (email OTP + ID upload + ops review) off that link on /forgot-password :: add the recovery entry point and its screens (upload, pending-review, rejected states) or re-home F-033
- [MUST] docs/12-dashboard-pages-spec.md :: D-20 bulk CSV accepts "≤ 5 000 lignes" while F-015 (docs/16) states "≤ 1 000 lignes v1" and a failure branch ">1 000 rows → Fichier trop grand — 1 000 lignes maximum" :: pick one row limit and align both docs (validation copy included)
- [MUST] docs/12-dashboard-pages-spec.md :: DM-35 Export roles are Owner/Admin/Finance while F-013 (docs/16) grants export to "Finance/Admin/Owner/Viewer (read)" :: align the export permission (decide whether Viewer may export) across D-12/D-19/D-29/DM-35 and F-013
- [MUST] docs/12-dashboard-pages-spec.md :: D-32/DM-18 API keys have no rotation grace-window UI, yet F-024 (docs/16) specifies rotate with a merchant-chosen grace window (0/24 h/72 h) and banner "Ancienne clé expire le {date}" :: add rotate action + grace-window picker + expiring-key banner to D-32, or strip rotation from F-024
- [MUST] docs/12-dashboard-pages-spec.md :: D-24 batch `pending_approval` has no expiry rule, while F-016 says an unapproved batch "expires to draft after 7 days" and reminder N-12 is re-sent at 48 h — no UI state or countdown reflects this :: specify batch-approval expiry (duration, banner for approvers/maker, resulting status) in D-24 and align with F-016
- [MUST] docs/12-dashboard-pages-spec.md :: D-14 link amount max is 10 000 000 FCFA and DM-04 charge max is 10 000 000, but docs/14 C-02 caps payer-entered amounts at 5 000 000 FCFA — a 10 M fixed link is creatable but the payer-side maximum contradicts it :: reconcile the amount ceilings (and tie them to tier limits) across D-14/DM-04/DM-13 and C-02
- [MUST] docs/12-dashboard-pages-spec.md :: D-13 "Désactiver" is Owner/Admin only, but docs/15 M-21/M-24/MS-06 allow Finance to deactivate links and F-011 (docs/16) says "Admin/Owner (creators may deactivate their own)" — three different role sets for the same action :: pick one deactivation permission model and align 12/15/16
- [MINOR] docs/12-dashboard-pages-spec.md :: D-12 status filter offers "multi-chips StatusBadge (8)" but charges can only be pending/succeeded/failed/expired/refunded — processing, pending_approval and draft never apply to charges :: restrict the transactions status filter to the 5 charge statuses
- [MINOR] docs/12-dashboard-pages-spec.md :: D-13 Payment links list has no search/filter input ("recherche via ⌘K" only) while docs/15 M-21 gives the same list a local title search :: add an inline search field to D-13 or justify the omission
- [MINOR] docs/12-dashboard-pages-spec.md :: D-19 Payouts tabs declare filters but no pagination for the "Paiements" and "Lots" tables (all other list pages state cursor pagination) :: state cursor pagination for both D-19 tables
- [MINOR] docs/12-dashboard-pages-spec.md :: D-18 Customer detail transactions table declares no pagination or empty state for the per-customer charge list :: add pagination + FR empty copy for the customer's transactions table
- [MINOR] docs/12-dashboard-pages-spec.md :: DM-16 has no failure state for `POST /topups` itself (network/server error while generating the reference) :: add an error state + retry to DM-16
- [MINOR] docs/12-dashboard-pages-spec.md :: Sidebar places "Abonnements" inside the Décaissements group, while docs/08 chrome lists Abonnements as its own group between Décaissements and Finances :: align the sidebar grouping with docs/08 or state the supersession explicitly
- [MINOR] docs/12-dashboard-pages-spec.md :: DM-02 refund reason list ("Erreur de montant / Produit indisponible / Demande du client / Autre") differs from F-012 ("Client insatisfait, Erreur de montant, Doublon, Autre") and MS-07 in docs/15 — three divergent reason taxonomies :: define one canonical refund-reason list and reference it from 12/15/16
- [MINOR] docs/12-dashboard-pages-spec.md :: D-11 quick action "Créer un lien" includes Developer, but the D-13 row action "Modifier" excludes Developer — a Developer can create but not edit a link with no stated rationale :: make link create/edit role sets coherent
- [MINOR] docs/12-dashboard-pages-spec.md :: F-012 states the refund modal must display "Les frais de 100 FCFA ne sont pas remboursés" but DM-02 contains no fee-not-returned copy :: add the fee-retention line to DM-02 contents
- [MINOR] docs/12-dashboard-pages-spec.md :: No UI is specified for F-068's go-live checklist (persisted per merchant, `GET/PUT /merchant/golive_checklist`) — D-10/D-11 checklists cover onboarding only and F-007 references a "go-live checklist modal" that exists nowhere in doc 12 :: add the go-live checklist modal (items, states, storage) to doc 12
- [MUST] docs/13-ops-console-spec.md :: Doc says internal endpoints are "to be specified in a future `docs/14-internal-api.md`" but docs/14 is already the checkout pages spec — numbering collision guarantees a broken reference :: point to an unused doc number (e.g. docs/17-internal-api.md)
- [MUST] docs/13-ops-console-spec.md :: KYB decision rights contradict docs/16 — O-03/O-04 restrict approve/reject to compliance (ops read-only) while F-045/F-046 name "ops" as the approving/rejecting actor :: align the KYB decision role between doc 13 and F-045/F-046
- [MUST] docs/13-ops-console-spec.md :: Tier-upgrade control contradicts docs/16 — OM-02 lets compliance approve a tier alone (DC only for custom limits) while F-047 requires "ops maker + ops-admin approver (dual-control above Tier 1)" per docs/08 §B :: state one dual-control rule for Tier 2 grants and align OM-02/O-06 with F-047
- [MUST] docs/13-ops-console-spec.md :: OM-05 suspension scope offers only a "Geler aussi les règlements" checkbox while F-048 specifies three scopes (collections only / all incl. payouts / full freeze) and a partial-suspension failure branch :: add the scope selector to OM-05 or reduce F-048 to the checkbox model
- [MUST] docs/13-ops-console-spec.md :: No 404 / 500 / maintenance / session-expired full pages are specified for the ops console (doc 12 defines D-45–D-48 for the dashboard; doc 13 has only a session-expired toast on O-01) :: add global error/maintenance/not-found states for ops.ijimpay.com
- [MINOR] docs/13-ops-console-spec.md :: O-03 (KYB queue), O-05 (merchants), O-12 tabs and O-15 (adjustments) tables state filters but no pagination, while O-07/O-18 explicitly use cursor pagination :: state pagination for every ops table
- [MINOR] docs/13-ops-console-spec.md :: O-01 uses `log-out` "(miroir)" as the SSO sign-in icon, violating docs/07 "one icon = one meaning"; doc 12 already introduces `log-in` for exactly this :: use `log-in` and add it to the icon-additions list
- [MINOR] docs/13-ops-console-spec.md :: O-09/OM-13 use `download` "(sens import)" for report import while docs/16 icon additions assign `upload` to "CSV/report import" — same glyph, opposite meanings :: use `upload` for imports
- [MINOR] docs/13-ops-console-spec.md :: OM-13 confirm rule "le mapping de colonnes doit être validé écran par écran" but the modal defines no mapping screens, fields, or error states :: specify the column-mapping step (fields, auto-map, errors) as in D-21
- [MINOR] docs/13-ops-console-spec.md :: O-13 risk case has no "request info from merchant" action although F-052 step 2 says the analyst "requests info from merchant if needed (email template)" and N-29 exists for it :: add a request-info action (template picker, deadline) to O-13
- [MINOR] docs/13-ops-console-spec.md :: O-19 does not encode F-054's rule "no hardware key within 7 days → account auto-disabled", nor any state for it :: add the auto-disable rule and its row state to O-19
- [MUST] docs/14-checkout-pages-spec.md :: No landing state for BOTH channels unavailable/down (merchant accepts both, `GET /channels` reports both down) — C-08 only covers one channel down; the payer would reach a dead CTA with no copy :: add a both-channels-down blocked state to C-01–C-04/C-08 with FR/EN copy
- [MUST] docs/14-checkout-pages-spec.md :: C-14/C-15 show the `refunded` badge "when a linked refund exists", but per docs/02 §2.1 and F-012 a PARTIAL refund leaves the charge `succeeded` — the receipt would mislabel partially-refunded payments as fully refunded :: specify partial-refund rendering (e.g. "Remboursé partiellement : {x} FCFA le {date}") distinct from full refund
- [MINOR] docs/14-checkout-pages-spec.md :: C-08 action calls `POST /v1/charges` while the doc's own API-additions table says payer-side creation goes through `POST /v1/public/charges` :: make C-08 (and C-13 retry) reference the public endpoint consistently
- [MINOR] docs/14-checkout-pages-spec.md :: C-11 does not implement F-039's rule "`payer_not_found` → retry disabled for same number+channel pair; forces change" — C-11 always offers plain « Réessayer » :: add the per-failure-code retry-gating rules from F-039 to C-11
- [MINOR] docs/14-checkout-pages-spec.md :: C-03 hardcodes quantity 1–99 while the API additions define a per-link `max_quantity` field that the validation never uses :: validate against `max_quantity` (fallback 99) and add the corresponding FR error
- [MINOR] docs/14-checkout-pages-spec.md :: C-19 widget chrome does not state whether the language toggle (global chrome) survives inside the iframe where the footer link is suppressed :: state widget-mode language-toggle behavior
- [MUST] docs/15-mobile-app-screens-spec.md :: Queued-write failure path is missing — M-22/outbox flush has no state for the server REJECTING a queued link at publish time (validation error, expired session, deactivated merchant); only the success toast is specified :: define flush-failure behavior (item kept with error chip, notification, edit-and-retry)
- [MUST] docs/15-mobile-app-screens-spec.md :: M-30/M-31 do not surface the approval locks defined in docs/16 — "payout approvals locked 24 h after PIN reset" (F-004) and "approvals allowed after 24 h device seasoning" (F-031) have no screen state or FR copy :: add locked-approval states (banner + disabled approve) to M-30/M-31 keyed to PIN-reset and device-seasoning windows
- [MUST] docs/15-mobile-app-screens-spec.md :: API additions contradict docs/12 and docs/16 — `POST /auth/otp/request` vs 12's `POST /auth/signup/otp`/`POST /auth/otp` vs API-ADD-1 `POST /auth/otp`; `POST /invitations` vs 12/16 `POST /invites`; three docs declare three different auth/team endpoint sets :: produce one canonical API-additions list and make 12/15/16 reference it
- [MUST] docs/15-mobile-app-screens-spec.md :: Link-create field rules contradict docs/12 — M-22 title 2–60 chars and description ≤ 200 vs D-14 title 2–80 and description ≤ 240 (same object, same API) :: align link field validations (title, description, min amount, expiry) between M-22 and D-14
- [MINOR] docs/15-mobile-app-screens-spec.md :: M-41 logo max 2 Mo vs D-38 logo max 1 Mo (and D-38 adds "carré ≥ 256 px" which M-41 phrases differently) :: align logo constraints and FR error copy across M-41/D-38
- [MINOR] docs/15-mobile-app-screens-spec.md :: Global conventions §StatusBadge contains a garbled self-contradiction ("draft shown as processing? No — draft renders `processing` label "Brouillon" is web-only") :: rewrite the draft-status rule as one clear sentence (app never renders draft; batches appear from pending_approval onward)
- [MINOR] docs/15-mobile-app-screens-spec.md :: MS-09 reject reason 5–200 chars vs DM-09 5–300 chars vs F-018 "≥ 10 chars" — three validations for the same reject-reason field :: pick one rule and align 12/15/16
- [MINOR] docs/15-mobile-app-screens-spec.md :: MS-10 role descriptions ("Finance — Encaisser, payer, rembourser") omit the maker–checker caveat present in DM-23 ("crée les paiements mais ne les approuve pas"), inviting wrong expectations on the surface where approvals happen :: align the Finance role explainer copy with DM-23
- [MINOR] docs/15-mobile-app-screens-spec.md :: MS-10 invite has no explicit offline behavior (M-40 covers list writes generically, but MS-10 is opened from a cached screen and its own States section is absent) :: state blocked-write (or queued) behavior + FR copy for MS-10
- [MUST] docs/16-flows-catalog.md :: No flow covers 2FA/TOTP enrollment (DM-26) even though F-017 web approval, DM-17 and D-41 all depend on TOTP being enrolled, and DM-08 blocks approval without it — the path from "no 2FA" to "can approve" is unflowed :: add a TOTP-enrollment flow (trigger, steps, failure branches: wrong code/clock skew, backup-code download, postconditions)
- [MINOR] docs/16-flows-catalog.md :: No flow for editing a payment link (F-011 covers only deactivate; `PATCH /payment_links/{id}` exists in 12/15) including the "montant non modifiable si paiements existent" rule :: add a link-edit flow or fold it into F-011 with its failure branch
- [MINOR] docs/16-flows-catalog.md :: F-046 rejection reason templates ("Document expiré", "Le nom ne correspond pas au RCCM"…) do not match the canonical OM-01 template library codes in docs/13 (`doc_illisible`, `rccm_expire`, …) :: reference the OM-01 library instead of restating divergent templates
- [MINOR] docs/16-flows-catalog.md :: F-007 says live keys are "revealed once" as a side effect of flipping the toggle, while D-32 (docs/12) only reveals keys via explicit DM-18 creation — two different reveal moments for live secrets :: specify whether go-live auto-creates a live key or routes to D-32
- [MINOR] docs/16-flows-catalog.md :: Header says routes/screens are "per docs/08, docs/09, docs/10" although docs 12/14/15 supersede those documents — flows should anchor to the superseding IDs (D-xx/C-xx/M-xx) :: update flow screen references to the new spec IDs
